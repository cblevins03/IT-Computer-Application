CREATE TABLE tablets (
    tabletid varchar(30) NOT NULL,
    tabletname varchar(30) NOT NULL,
    tabletos varchar(30) NOT NULL,
    tabletip varchar(20) NOT NULL,
    tabletsernum varchar(25) NOT NULL,
    tabletdatepurch varchar(25) NOT NULL,
    tabletstatus varchar(30) NOT NULL,
    tabletnotes varchar(300) NOT NULL,
);
